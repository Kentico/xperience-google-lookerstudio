{
  "devDependencies": {
    "@google/clasp": "^2.4.1",
    "eslint": "^8.17.0"
  },
  "scripts": {
    "push": "npm run lint && cd src/connector && clasp push",
    "lint": "npx eslint src/connector/script/*.js --fix"
  }
}
